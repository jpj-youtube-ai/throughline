node_modules/
out/
.next/
.clones/
*.log
*.tsbuildinfo
next-env.d.ts

# secrets — .env.example IS committed
.env
.env*.local

# local preview / e2e artifacts
.playwright-mcp/
*.local.png

# superpowers SDD scratch (progress ledger, briefs, review packages)
.superpowers/

# Claude Code deploy worktrees (the live web + worker run from here) — never
# commit these; the shareable config under .claude/ (settings.json, hooks,
# agents, skills) IS tracked.
.claude/worktrees/
.claire/worktrees/
